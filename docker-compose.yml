services:
  app:
    image: poker-app:${APP_VERSION:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3007:3007"
    environment:
      - HOST=0.0.0.0
      - PORT=3007
      - NODE_ENV=production
    restart: unless-stopped
