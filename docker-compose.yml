services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: poker-app
    ports:
      - "3007:3007"
    environment:
      - HOST=0.0.0.0
      - PORT=3007
      - NODE_ENV=production
    restart: unless-stopped
