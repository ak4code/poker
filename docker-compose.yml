services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: poker-app
    ports:
      - "3007:3000"
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
